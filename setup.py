from setuptools import setup

setup(
    name='eq-open',
    version='1.0.0',
    author='exp(Quant)',
    author_email='ivan@expquant.com',
    url='https://github.com/exp-Quant/EQOpen',
    install_requires=[],
    packages=['eqopen'],
)
